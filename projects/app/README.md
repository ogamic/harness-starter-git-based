# app — web client (example stub)

A worked example sub-project. Replace it with your own web client, or delete it once you've learned the pattern.

- **Stack:** React · Vite · TypeScript · Tailwind
- **Owned by:** the `app` sub-agent (`../../management/agents/app.md`)
- **Its law:** [`documents/`](documents/) — architecture, coding conventions, and the format the agent reports back in.

Consumes the `api` service's shapes; it does not define them.
