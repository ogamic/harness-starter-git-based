# OS / editor cruft
.DS_Store
*.swp
.idea/
.vscode/

# Secrets / local config
.env
.env.*
!.env.example

# ── Machine-local harness settings ──
# Matched at any depth: the harness dirs live under management/, not the repo root.
**/.claude/settings.local.json
**/.codex/config.local.toml

# Dependencies / build output (per sub-project)
node_modules/
dist/
build/
